fastapi==0.115.12
uvicorn==0.34.0
python-multipart==0.0.20
numpy==1.26.4
# PP-OCR detection+recognition on ONNXRuntime — same models as PaddleOCR but no
# paddle runtime, so no macOS "No allocator found for the place" crash.
rapidocr-onnxruntime==1.4.4
# Vietnamese recognition model (runs on the crops RapidOCR detects).
# vietocr does not declare torch — install it explicitly.
vietocr==0.3.13
torch==2.8.0
torchvision==0.23.0
